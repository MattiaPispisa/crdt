name: crdt_lf_drift
description: drift storage adapter for CRDT LF library objects, providing persistence for Change and Snapshot objects.
homepage: https://mattiapispisa.github.io/
repository: https://github.com/MattiaPispisa/crdt/tree/main/packages/crdt_lf_drift
topics: [crdt, local-first, drift, persistence, dart]

version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  crdt_lf: "^3.0.0"
  drift: ^2.34.0
  hlc_dart: ^1.0.0

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.34.0
  test: ^1.24.0
