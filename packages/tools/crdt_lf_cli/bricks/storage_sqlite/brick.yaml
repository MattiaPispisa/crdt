name: storage_sqlite
description: "A storage backend on SQLite."
version: 0.1.0+1

environment:
  mason: ">=0.1.0 <0.2.0"

vars:
  name:
    type: string
    description: The project name, in snake_case.
    prompt: What is the project name?
