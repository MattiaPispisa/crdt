name: crdt_lf_cli
description: A command-line tool that generates crdt_lf projects - a sync server, a Flutter client and the storage behind them.
homepage: https://mattiapispisa.github.io/
repository: https://github.com/MattiaPispisa/crdt/tree/main/packages/tools/crdt_lf_cli
topics: [crdt, local-first, cli, codegen, template]

version: 0.1.0

environment:
  sdk: ^3.6.0

executables:
  crdt_lf:

dependencies:
  args: ^2.7.0
  mason: ^0.1.3
  path: ^1.9.0

dev_dependencies:
  test: ^1.24.0
