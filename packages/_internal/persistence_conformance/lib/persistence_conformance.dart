/// The conformance suite every `crdt_lf_persistence` storage adapter is
/// checked against, and the in-memory storage the suite checks itself against.
library;

export 'src/conformance.dart';
export 'src/in_memory_storage.dart';
