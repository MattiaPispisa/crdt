name: benchmark_infrastructure
description: >-
  Private, unpublished package holding the shared benchmark-running
  infrastructure reused by every package's `benchmarks/` folder.
publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  benchmark_harness: ^2.3.1

dev_dependencies:
  test: ^1.24.0
